trigger: none
pr: none

variables:
  imageTag: v1.2

parameters:
  - name: builds
    type: object
    default:
      - appName: frontend
        imageTag: ${{ variables.imageTag }}  # ✅ Works!
      - appName: backend
        imageTag: ${{ variables.imageTag }}  # ✅ Works!
